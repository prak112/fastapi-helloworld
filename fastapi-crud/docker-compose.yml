version: '2.0'

services:
  web:
    build: ./fastapi-crud/src
    command: uvicorn app.main:app --reload --workers 1 --host 0.0.0.0 --port 8000
    volumes:
      - ./fastapi-crud/src/:/usr/fastapi-crud/src/app/
    ports:
      - 8002:8000